"""
The print command accepts a single parameter and prints it to standard output.

Standard output is what we call the command line interface (terminal on a Mac).

Note that this is the same thing, but we have wrapped the strings in DOUBLE
quotes instead of single quotes.
"""


print("Everything flows and nothing stays")
print("A multitude of words is no proof of a prudent mind")
print("Hope is the most universal of human possessions")
