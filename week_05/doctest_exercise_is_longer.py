def is_longer(first_list, second_list):
    """
    Return True if and only if the length of first_list
    is greater than the length of second_list.

    :param first_list: a list of course!
    :param second_list: a list of course!
    :postcondition: neither first_list nor second_list are modified
    :return: True if first_list is longer, else False

    >>> is_longer([1, 2, 3], [4, 5])
    True
    >>> is_longer(['abcdef'], ['ab', 'cd', 'ef'])
    False
    >>> is_longer(['a', 'b', 'c'], [1, 2, 3])
    False
    """
